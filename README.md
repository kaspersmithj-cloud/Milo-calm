# Milo Trainer Pro Clean Fix

This package goes back to the normal filenames:

- index.html
- app.js
- styles.css
- programme.js
- manifest.json
- sw.js

Important:
1. Delete the old `*-v31` files from GitHub.
2. Upload these files and overwrite the old normal files.
3. Commit changes.
4. Open the site with `?fresh=4` once:
   https://kaspersmithj-cloud.github.io/Milo-calm/?fresh=4

This version uses vertical task cards so the checklist cannot squeeze into a narrow right-hand column.
